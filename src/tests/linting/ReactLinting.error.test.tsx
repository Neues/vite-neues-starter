import { useEffect, useRef } from 'react';

const Component = () => {
	<div children='Children' role='datepicker' />;
	if (true) {
		useEffect(() => {});
	}
	const ref = useRef(null);
	const value = ref.current;
	return value;
};

export default Component;
